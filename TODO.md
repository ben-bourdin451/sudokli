# TODO

- [ ] Difficulty Rating Model (heuristic scoring system)
- [ ] Testing & Validation Suite (property tests, large batch generation checks)
- [ ] Error counter should check against the known solution instead of current validation. It doesn't always show errors correctly.
